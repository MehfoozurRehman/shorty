{
  "name": "shorty",
  "type": "module",
  "scripts": {
    "dev": "tsx watch index.ts",
    "start": "prisma generate && npx tsx index.ts",
    "generate": "prisma generate",
    "studio": "prisma studio",
    "format": "npx prettier --write ."
  },
  "dependencies": {
    "@hono/node-server": "^1.19.6",
    "@prisma/client": "^5.22.0",
    "hono": "^4.10.7"
  },
  "devDependencies": {
    "@types/node": "^20.19.25",
    "prisma": "^5.22.0",
    "tsx": "^4.21.0"
  }
}
